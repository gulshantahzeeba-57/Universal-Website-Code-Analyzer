# Copy this file to .env locally and fill in your real key.
# Never commit the real .env — it's already excluded in .gitignore.
# On Vercel (or any host), set this as an Environment Variable in the
# dashboard instead of uploading a file.

GROQ_API_KEY=your_key_here
